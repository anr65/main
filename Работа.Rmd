---
title: "Работа"
author: "Утята"
date: "4/12/2021"
output: html_document
---

```{r}
library(DescTools)
library(EnvStats)
library(outliers)
library(ggplot2)
library(ggpubr)
library(Hmisc)
library(corrplot)
library(rio)
library(robustHD)
library(normtest)
library(GGally)
library(leaps)
library(tidyr)
library(sjPlot)
library(dplyr)

data <- read.csv('datas1.csv')
data <- data[rowSums(is.na(data)) == 0,]
data <- select(data, -X)
data <- select(data, -sl_no)
data
```

```{r}
Desc(data)
```
Выделим данные объясняющей переменной
```{r}
data_cont <- select(data, -salary)
summaryStats(data_cont)
```
Присвоим целевое целевое значение переменной Y
```{r}
Y <- data$salary
Desc(Y)
```
## Проверка IQR

```{r}
IQR(Y)/2/median(Y)
```
Данные однородны

